<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.arounders.web.repository.BoardRepository">
    <select id="getList" resultType="Board">
        select * from BOARD WHERE IS_HIDDEN = 0
    </select>

    <select id="getHiddenList" resultType="Board">
        select * from BOARD WHERE IS_HIDDEN = 1
    </select>

    <select id="getBoard" parameterType="java.lang.Integer" resultType="Board">
        select * from BOARD where id = #{id} AND IS_HIDDEN = 0
    </select>

    <select id="getHiddenBoard" parameterType="java.lang.Integer" resultType="Board">
        select * from BOARD where id = #{id} AND IS_HIDDEN = 1
    </select>

    <insert id="insert" parameterType="Board">
        INSERT INTO BOARD(TITLE, CONTENT, REGION, CREATED_AT, MEMBER_ID, CATEGORY_ID)
        VALUES(#{title}, #{content}, #{region}, NOW(), #{memberId}, #{categoryId})
        <selectKey resultType="java.lang.Long" keyProperty="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <update id="update" parameterType="Board">
        UPDATE BOARD SET TITLE = #{title}, CONTENT = #{content}, MEMBER_ID = #{memberId}, UPDATED_AT = NOW() WHERE ID = #{id}
    </update>

    <update id="renew" parameterType="java.lang.Object">
        UPDATE BOARD SET THUMBNAIL_NAME = #{param1}, THUMBNAIL_PATH = #{param2} WHERE ID = #{param3}
    </update>

    <delete id="delete" parameterType="java.lang.Integer">
        DELETE FROM BOARD WHERE ID = #{id}
    </delete>
</mapper>